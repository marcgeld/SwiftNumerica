name: CI

on:
  push:
    branches:
      - main
      - "codex/**"
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build, test, and validate package consumption
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Show Swift version
        run: swift --version

      - name: Describe package
        run: swift package describe

      - name: Build
        run: swift build

      - name: Test
        run: swift test

      - name: Create consumer package
        run: |
          set -euo pipefail

          CONSUMER_DIR="$(mktemp -d)"
          cd "$CONSUMER_DIR"

          cat > Package.swift <<EOF
          // swift-tools-version: 6.2

          import PackageDescription

          let package = Package(
              name: "SwiftNumericaConsumer",
              platforms: [
                  .macOS(.v14)
              ],
              dependencies: [
                  .package(path: "${GITHUB_WORKSPACE}")
              ],
              targets: [
                  .executableTarget(
                      name: "SwiftNumericaConsumer",
                      dependencies: [
                          .product(name: "SwiftNumerica", package: "SwiftNumerica")
                      ]
                  )
              ],
              swiftLanguageModes: [.v6]
          )
          EOF

          mkdir -p Sources/SwiftNumericaConsumer
          cat > Sources/SwiftNumericaConsumer/main.swift <<'EOF'
          import SwiftNumerica

          let tensor = Tensor.vector([1, 2, 3, 4])
          guard Numerica.Statistics.mean(tensor) == 2.5 else {
              fatalError("SwiftNumerica import or mean calculation failed")
          }
          EOF

          swift run

      - name: Build and run examples
        working-directory: Examples
        run: |
          set -euo pipefail

          swift build

          for directory in Sources/*/; do
            example="$(basename "$directory")"
            echo "::group::${example}"
            swift run -q "$example"
            echo "::endgroup::"
          done
